import { PAGE_SECTIONS } from '../temp/pageSections.js';

export const PageSections = (state = PAGE_SECTIONS, action) => {
    switch (action.type) {
        default:
          return state;
      }
};