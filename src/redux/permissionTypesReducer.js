import { PERMISSION_TYPES } from '../temp/permissionTypes.js';

export const PTypes = (state = PERMISSION_TYPES, action) => {
    switch (action.type) {
        default:
          return state;
      }
};